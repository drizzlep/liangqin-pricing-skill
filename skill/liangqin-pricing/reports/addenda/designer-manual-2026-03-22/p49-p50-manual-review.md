# p49-p50 图片残留文字人工校对

- source_pages: `49`, `50`
- source_image_dir: `pdf-block-review-2026-03-24/page-images`
- related_block_dir: `pdf-block-review-2026-03-24/block-images`
- review_goal: 补齐 `p49/p50` 在图片中的残留文字，并区分“已覆盖规则 / 待新增候选 / 结构说明”

## 结论摘要

- `p50` 已明确识别出一条有效硬规则：`门盖牙称与顶挡条时最少需要留出15mm`。
- 这条规则已经被现有 runtime 覆盖，对应当前结论仍然成立：`已覆盖，不重复入库`。
- `p49` 主要是 `直角圆边-窄边高柜` 的结构示意页，图片里能看清 `凹槽内退尺寸` 及若干尺寸标注，但当前更像结构设计规则，不足以直接认定为新的报价 runtime 规则。
- `p49` 最值得后续人工深挖的内容，是图中能读到的 `凹槽内退尺寸` 及相关尺寸 `20 / 12 / 8 / 6`；这部分应先整理成清晰文字，再决定是否进入“设计限制规则层”。
- 进一步放大后，`p49` 左侧两张图已经能读出更明确的尺寸关系：
  - 上图能看到一处竖向尺寸 `20`，右侧窄边结构顶部并列标出 `8` 与 `12`
  - 下图能看到 `凹槽内退尺寸` 字样，旁边标出 `8`，顶部标出 `12`，最外侧圆边处标出 `6`
- 结合 `p43/p44` 的文字层规则后，`p49` 可以上升为“结构约束句草稿（待确认）”：
  - 这一步仍只进入人工复核层，不进入 runtime
  - 草稿中的“半盖铰链 / 掩盖值 8mm / 凹槽内退按公式计算”来自相邻规则页推导，不是 `p49` 单页直读
- 再结合 `p49` 的两张实物圆图与后文挡条规则，`p49` 上节点的 `20` 现在可以做更具体的高置信推断：
  - 它大概率对应门上方顶挡条的可见高度/宽度，而不是普通背景尺寸
  - 但“高度”还是“宽度”的术语口径，当前仍保守不写死
- 相邻页 `p48` 为 `直角圆边-窄边矮柜`，并展示了同一套窄边节点做法：
  - 可更清楚地看到 `12`、`12`、`8` 这组尺寸关系
  - 同样出现 `凹槽内退尺寸`
  - 这说明 `p49` 不是孤立信息，而是“窄边矮柜 / 窄边高柜”成组结构规则中的高柜页

## 相邻页上下文

### p48

- 页面标题：`直角圆边-窄边矮柜`
- 与 `p49` 的关系：同一组 `直角圆边-窄边` 结构页，只是对象从 `矮柜` 换成了 `高柜`
- 相邻页能稳定读出的信息：
  - 圆边节点图两处外侧平直段都可见 `12`
  - 节点内退/掩盖位置可见 `8`
  - 同样带有 `凹槽内退尺寸`
- 作用：
  - 为 `p49` 的尺寸解读提供旁证
  - 说明 `12 / 12 / 8` 不是 OCR 偶然噪声，而是这一组窄边节点的重复结构尺寸
- 当前判断：
  - `p48` 和 `p49` 更像结构设计约束页
  - 若后续要把 `p49` 转成完整规则句，最好把 `p48/p49` 合并理解，而不是只看单页

## 页面校对

### p49

- 页面标题：`直角圆边-窄边高柜`
- 页面性质：结构示意 / 柜体节点细节页
- 页面直读能稳定识别的内容：
  - `凹槽内退尺寸`
  - 尺寸标注可见 `20`
  - 尺寸标注可见 `12`
  - 尺寸标注可见 `8`
  - 尺寸标注可见 `6`
- 放大后的人工可读关系：
  - 上方节点线稿：可见一处竖向尺寸 `20`
  - 上方节点线稿右侧：顶部并列标出 `8`、`12`
  - 下方节点示意图：`凹槽内退尺寸` 对应位置标出 `8`
  - 下方顶部尺寸标为 `12`
  - 下方最外侧圆边位置标出 `6`
- 来自相邻页与前序规则页的推导信息：
  - `p43` 文字层可直接读出：`直角圆边-窄边` 属于 `窄边框柜体`，`矮柜 / 高柜门板均用半盖铰链，掩盖值为8mm`
  - `p44` 文字层可直接读出：圆边窄边体系存在公式 `凹槽内退尺寸 = 边角类型（6mm） + 门板厚度 + 2mm/5mm(按弹)`
  - `p48` 与 `p49` 为同一家族页，`p48` 可重复看到 `12 / 12 / 8` 与 `凹槽内退尺寸`
  - `p49` 上方两张实物圆图都能看到门上方有一条独立横向挡条，和上节点线稿的 `20 / 8 / 12` 位置关系一致
  - 后文挡条章节可直接读出：
    - `圆弧挡条——门板盖顶挡条`
    - `当门板盖顶挡条时，建议外露15mm`
    - `圆弧挡条 ... 20mm≤宽度≤120mm`
- 推导边界说明：
  - `p43` 对 `直角圆边-窄边` 的“半盖铰链、掩盖值 8mm”支持较强
  - `p44` 明示的是 `圆角圆边-窄边` 公式，不是 `直角圆边-窄边` 单页明示
  - 后文挡条章节并不是 `p49` 同页文字，但它与 `p49` 上节点实物图、`p50` 的 `门盖...顶挡条最少留15mm` 在结构关系上高度吻合
  - 因此把 `p44` 公式迁移到 `p48/p49` 只能算“同一圆边窄边体系下的高置信旁证”，还不能当作 `p49` 单页直接结论
- 人工判断：
  - 这页重点不是报价句式，而是窄边高柜节点结构尺寸。
  - 它说明“窄边高柜”在直角圆边做法下存在明确的凹槽内退和边距尺寸要求。
  - 结合相邻页 `p48`，可以更有把握地认为 `20 / 12 / 8 / 6` 都是真实结构尺寸，而非 OCR 杂讯。
  - 目前已经能较稳地判断：`20` 来自上节点门上方顶挡条区域，极可能对应顶挡条的可见高度/宽度；`12` 与 `6` 来自下节点外侧圆边做法，`8` 与 `凹槽内退尺寸` 对应。
  - 但仍不能 100% 稳定地还原成完整规范句，例如“`20mm` 在图纸口径里究竟记为顶挡条高度、宽度还是外露尺寸”“下节点 `12mm` 是平直段宽度还是面宽定义”，所以还不宜直接写成确定性 runtime 规则。
- 当前归类建议：`needs_manual_judgement`
- 原因：
  - 有真实信息，不应当视为噪声。
  - 但它更像设计/结构约束，不宜直接按报价 runtime 规则入库。

## 结构约束句草稿（待确认）

### p48 / p49 联合草稿

- 草稿用途：进入“设计规则草稿层/人工复核层”，不直接进入 runtime
- 草稿句：
  - `直角圆边-窄边矮柜 / 高柜` 属于 `窄边框柜体` 的圆边节点做法，门板应采用半盖铰链，掩盖值按 `8mm` 控制。
  - 该组节点存在明确的 `凹槽内退尺寸` 约束；已直读坐实的节点尺寸至少包括：`p48` 的 `12 / 12 / 8`，以及 `p49` 的上节点 `20 / 8 / 12`、下节点 `12 / 6 / 8`。
  - `p49` 上节点所对应的实物做法大概率是“门盖顶挡条”的窄边圆角节点，其中 `20mm` 很可能描述顶挡条的可见高度/宽度。
  - 结合圆边窄边体系相邻规则页，`凹槽内退尺寸` 很可能按“边角类型值 + 门板厚度 + 2mm/5mm 开启余量”计算；其中 `6mm` 大概率对应圆边边角类型值。
- 直接可见证据：
  - `p48` / `p49` 标题分别为 `直角圆边-窄边矮柜`、`直角圆边-窄边高柜`
  - 两页都能直接看到 `凹槽内退尺寸`
  - `p48` 圆边节点可直接读到 `12 / 12 / 8`
  - `p49` 上节点可直接读到 `20 / 8 / 12`
  - `p49` 下节点可直接读到 `12 / 6 / 8`
- 推导补强证据：
  - `p43` 明确给出 `直角圆边-窄边` 属于窄边框柜体，且矮柜/高柜门板均用半盖铰链、掩盖值 `8mm`
  - `p44` 明确给出圆边窄边体系的 `凹槽内退尺寸` 计算公式与 `6mm` 圆边类型值示例
  - `p49` 两张实物圆图可见门上方独立横向挡条
  - 后文挡条章节给出 `门板盖顶挡条`、`建议外露15mm`、`宽度下限20mm` 三组文字线索，与 `p49/p50` 高度吻合
- 暂不确定点：
  - `20mm` 的确切术语口径仍未锁定，目前更倾向于“顶挡条可见高度/宽度”，但暂不区分是高度、宽度还是外露尺寸
  - `12mm` 在 `p48/p49` 中更像圆边节点的外侧平直段尺寸，但仍需结合更清晰原图或人工标注确认
  - `8mm` 虽与 `p43` 的掩盖值高度吻合，但当前仍属于高置信推导，不是 `p48/p49` 单页文字层明说
  - `p44` 的公式是否可以无条件平移到 `直角圆边-窄边`，当前只能给出高置信推定，不能当作定论

### p50

- 页面标题：`窄边风格——拆装注意事项`
- 页面性质：拆装限制 / 明确硬约束页
- 图片中可稳定读出的完整文字：
  - `门盖牙称与顶挡条时最少需要留出15mm`
- 人工判断：
  - 这是清晰、可执行、可结构化的规则句。
  - 现有 runtime 中的 `窄边风格拆装时门盖牙称与顶挡条最少留出15mm` 已经正确吸收了这条内容。
  - 其余四个局部图主要用于解释不同节点做法，不构成新的独立报价规则。
- 当前归类建议：`covered_runtime`
- 原因：
  - 规则本体已被收录。
  - 本页剩余信息以结构示意为主，不建议继续拆成额外 runtime 规则。

## 图块级人工说明

| page | block_id | 当前 OCR | 人工识别 | 建议状态 |
| --- | --- | --- | --- | --- |
| 49 | `p049-b04` | `BeHY-sy / Se / 高 容-或圆甫直` | `直角圆边-窄边高柜` | `needs_manual_judgement` |
| 49 | `p049-b05` | 整页兜底块 | `直角圆边-窄边高柜` + `凹槽内退尺寸` + 上节点 `20/8/12` + 下节点 `12/6/8`；可升格为“结构约束句草稿（待确认）” | `needs_manual_judgement` |
| 50 | `p050-b06` | `... m出留要需是t盖 ...` | `窄边风格——拆装注意事项`；`门盖牙称与顶挡条时最少需要留出15mm` | `covered_runtime` |
| 50 | `p050-b07` | 整页兜底块 | 同上，整页结构说明辅助图 | `covered_runtime` |

## 下一步建议

1. `p50` 不再作为新增缺漏推进，只保留为“已覆盖核验通过”。
2. `p49` 现在可以先升格到“结构约束句草稿层”，但仍不建议直接进入 runtime。
3. 如果下一轮继续深挖，建议只做一件事：
   - 把 `p48/p49` 作为一组联合整理，确认 `20`、`8`、`12`、`6` 分别对应哪一段板件/边距，再把草稿收敛成完整约束句。
4. 在 `p49` 的尺寸主语没有被明确之前，不建议直接新增 runtime。
