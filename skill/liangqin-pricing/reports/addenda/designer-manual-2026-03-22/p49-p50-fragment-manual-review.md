# p49-p50 碎片图块人工处置说明

- review_scope: `p049-b01`、`p049-b02`、`p049-b03`、`p050-b01`、`p050-b02`、`p050-b03`、`p050-b04`、`p050-b05`
- review_goal: 把 `p49/p50` 剩余碎片块从“待判断高风险”中分流为“已并入主块”或“背景噪声”
- decision_principle:
  - 能为 `p049-b05` 的结构草稿提供局部尺寸旁证的，标为 `covered_non_runtime`
  - 仅包含木纹边角、放大圈轮廓、局部线稿残片的，标为 `non_rule_background`
  - 不把这类碎片继续当成独立规则候选

## p49

### `p049-b01`

- 图像内容：上节点局部截图，可见 `凹槽内退尺寸` 的竖排局部和 `8/15` 尺寸残片
- 结论：不构成独立规则句，但属于真实节点尺寸证据
- 建议状态：`covered_non_runtime`
- 处理方式：并入 `p049-b05` 的结构约束草稿证据链

### `p049-b02`

- 图像内容：实物图圆角与柜门边框局部，主要是放大圈轮廓和木纹边角
- 结论：没有稳定文字或独立尺寸信息
- 建议状态：`non_rule_background`
- 处理方式：保留截图归档，不再单独复核

### `p049-b03`

- 图像内容：下节点局部，只剩一小段 `8/15` 尺寸残片
- 结论：不能独立成句，但与 `p049-b05` 的节点尺寸判断一致
- 建议状态：`covered_non_runtime`
- 处理方式：并入 `p049-b05` 的结构约束草稿证据链

## p50

### 总体判断

- `p50` 的核心规则已经由 `p050-b06/p050-b07` 确认为 `covered_runtime`
- `p050-b01..b05` 都只是节点图中的局部边角、放大圈轮廓或线稿残片
- 它们不再适合继续保留在高风险清单中

### `p050-b01`

- 内容：左上节点实物图边角局部
- 建议状态：`non_rule_background`

### `p050-b02`

- 内容：右上节点实物图边角局部
- 建议状态：`non_rule_background`

### `p050-b03`

- 内容：左上节点示意线局部
- 建议状态：`non_rule_background`

### `p050-b04`

- 内容：左下节点放大圈边缘
- 建议状态：`non_rule_background`

### `p050-b05`

- 内容：右下节点柜门边角局部
- 建议状态：`non_rule_background`

## 预期影响

- `p49/p50` 的碎片块不再占用高风险复杂页清单
- `p49` 只保留真正值得继续看的 `p049-b04`、`p049-b05`
- `p50` 只保留已确认覆盖 runtime 的 `p050-b06`、`p050-b07`
