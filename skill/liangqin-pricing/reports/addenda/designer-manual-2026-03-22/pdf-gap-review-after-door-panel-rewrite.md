# 设计师手册剩余缺漏复核结论

- review_date: 2026-03-23
- base_audit_file: `pdf-coverage-audit.csv`
- current_runtime_file: `runtime-rules.json`
- current_runtime_rule_count: 94

## 结论摘要

- `pdf-coverage-audit.csv` 中仍有 `15` 条 `manual_review + pricing_relevant=True`。
- 但这 `15` 条里，并不等于还有 `15` 条真正未补入运行时。
- 其中 `p191-p192` 与 `p202-p208` 已经通过“门型规则簇重写”补入 runtime，不再建议逐页照搬。
- `p179` 的“平板平开门尺寸限制快速检索表”主要是已存在详细门型规则的总览页，当前不视为新的高价值缺口。
- `p49/p50` 相关页已先落地其中最清晰的一条硬约束，其余图示和结构说明继续保留人工复核。
- 同时，原 runtime 中与这两组门型碎片页对应的旧噪声规则 `p188`、`p209` 已完成清理，不再与新结构化门型规则并存。
- 原 runtime 中 `p31/p34` 的旧边角噪声标题也已合并清洗为一条结构化柜体规则，不再保留 OCR 碎片标题。
- 原 runtime 中 `p128` 的旧 `走线圆口` 噪声标题也已清洗为结构化规则。
- 原 runtime 中 `p127` 的旧榻榻米托称弱标题已清洗为结构化规则。
- 原 runtime 中 `p130` 的旧柜侧开口/缺口 OCR 碎片标题已清洗为结构化规则。
- 原 runtime 中 `p55` 的常规拆装柜体弱标题已拆分为结构化规则。
- 原 runtime 中 `p59` 的开放格分段缝弱标题已清洗为结构化规则。
- 原 runtime 中 `p85` 的遇见书柜结构弱标题已清洗为结构化规则。
- 原 runtime 中 `p159` 的旧抽屉开启方式碎片标题也已清洗为结构化规则。
- 原 runtime 中 `p97` 的旧 `注意轨道` 书梯碎片标题也已清洗为结构化规则。
- 原 runtime 中 `p170` 的旧托盘抽开启方式碎片标题也已清洗为结构化规则。
- 原 runtime 中 `p289` 的旧手扫雷达开关弱标题也已清洗为结构化配件规则。

## 已通过结构化规则覆盖的残留页

### 铝框平开门快速检索表

- CSV 残留页: `p191`, `p192`
- 处理方式: 不按碎片页入库，改为结构化 `door_panel` runtime 规则
- 已落地 runtime:
- `p196` `针式铰链铝框门尺寸限制`
- `p197` `铝框岩板门尺寸限制`
- 说明: `常规铰链铝框门`、`天地铰链铝框门` 的详细规则原本已存在，本轮只补齐缺失子门型

### 拼框平开门快速检索表

- CSV 残留页: `p202`, `p203`, `p204`, `p205`, `p206`, `p207`, `p208`
- 处理方式: 不按碎片页入库，改为结构化 `door_panel` runtime 规则
- 已落地 runtime:
- `p216` `超高拼框木门尺寸限制`
- `p217` `拱形玻璃门尺寸限制`
- `p217` `胶囊玻璃门尺寸限制`
- `p218` `超高拼框玻璃门尺寸限制`
- `p221` `藤编门尺寸限制`
- `p221` `拱形藤编门尺寸限制`
- `p223` `美式木门尺寸限制`
- `p224` `美式玻璃门尺寸限制`

## 已有详细规则覆盖但不建议重复入库

### 平板平开门快速检索表

- CSV 残留页: `p179`
- 结论: 当前不视为新的高价值缺口
- 原因:
- 这页更像流云门、飞瀑门、拉线门、月亮门、骨骼线门、零食柜门的总览表
- 对应详细规则页已在 `p180-p187` 区间内存在并已进入 runtime
- 再把总览页硬塞进 runtime，容易重复命中且增加噪声

## 仍保留人工复核的条目

### 部分已落地，剩余信息继续人工复核

- `p49/p50` `窄边风格——拆装注意事项`
- 已落地:
- `窄边风格拆装时门盖牙称与顶挡条最少留出15mm`
- 仍保留人工复核部分:
- 其余场景图、结构示意、凹槽内退尺寸等内容当前抽取仍不稳定，暂不直接程序化

### 已完成结构化替代的边角规则

- 原旧 runtime 条目:
- `p31` 噪声标题 `新现代边角-侧边平齐 ...`
- `p34` 说明型标题 `为方便查看尺寸，尺寸图中边角为直边...`
- 已替代为:
- `新现代边角风格柜体默认顶盖侧结构，侧盖顶需特殊备注`

### 已完成结构化替代的开口规则

- 原旧 runtime 条目:
- `p128` 噪声标题 `层板 层板 ... 走线圆口`
- 已替代为:
- `走线圆口规格与距边要求`

### 已完成结构化替代的托称与柜侧开口规则

- 原旧 runtime 条目:
- `p127` 弱标题 `榻榻米组合柜空区适用于该托称添加规则，但添加托称的组合柜需配合 固定上墙`
- `p130` OCR 碎片标题 `参考铰链`
- `p130` OCR 碎片标题 `方剩余板件宽度不小于侧板宽度的1`
- 已替代为:
- `榻榻米组合柜空区加托称时需固定上墙`
- `柜侧前开口尺寸限制`
- `柜侧前缺口尺寸限制`
- `柜侧闭合缺口尺寸限制`

### 已完成结构化替代的常规拆装柜体规则

- 原旧 runtime 条目:
- `p55` 弱标题 `其他特殊情况可调整牙称高度`
- 已替代为:
- `常规拆装柜体高度≤1700mm默认顶盖侧，＞1700mm默认侧盖顶`
- `常规拆装柜体牙称常用50/80mm，允许范围50-250mm`

### 已完成结构化替代的开放格与遇见书柜规则

- 原旧 runtime 条目:
- `p59` 弱标题 `情况开放格区域层板内凹，层板外沿与门板不齐平。为提高家具结构稳定性及保`
- `p85` 弱标题 `遇见书柜-上柜顶底包侧`
- 已替代为:
- `超高带门柜体开放格分段缝优先对齐层板上方`
- `遇见书柜下柜高度超过1700mm时不建议做侧包顶底`

### 已完成结构化替代的抽屉规则

- 原旧 runtime 条目:
- `p159` 模糊标题 `可使用扣手、明装拉手`
- 已替代为:
- `玻璃抽屉尺寸与开启方式限制`

### 已完成结构化替代的书梯规则

- 原旧 runtime 条目:
- `p97` 模糊标题 `注意轨道`
- 已替代为:
- `书梯书柜尺寸与滑轨安装要求`

### 已完成结构化替代的托盘抽规则

- 原旧 runtime 条目:
- `p170` 模糊标题 `可使用扣手、明装拉手`
- 已替代为:
- `托盘抽尺寸与开启方式限制`

### 已完成结构化替代的配件规则

- 原旧 runtime 条目:
- `p289` 弱标题 `适用于所有型号的单色温灯带`
- 已替代为:
- `可隔门板手扫雷达开关适用材质与灯带范围`

### 噪声或背景说明

- `p1` `良禽佳木设计师标准手册`
- `p20` `N Ne i 5 —— Se A dare le |`
- `p78` `1-15045 25 150 40DG-02 DG-06 DG-10`
- `p284` `2onj2 QGWwouztisf2 [Pe Lwgl]e2fIcG' UGIGELIC SUG GXfLIOLGIUSLA . i —e`
- 结论: 继续排除在 runtime 之外

## 当前剩余高价值缺口判断

- 就“新追加报价体系”的当前范围而言，本轮补完后暂无新的高价值 runtime 缺口需要立刻实现。
- 若按“还能直接新增进体系的新规则”统计，当前判断为 `0` 条。
- 若按“runtime 里仍值得继续结构化清洗的弱规则”统计，当前为 `0` 条。
- 后续若继续扩展，优先级建议为:
- 对现有 runtime 中已知噪声条目进行降噪或降级，而不是继续横向加新规则
- 若要继续深挖窄边风格，再回原 PDF 人工整理“凹槽内退尺寸”等可量化规则
